import * as React from "react"
import { ImportIdentity } from "./ui"

interface PlaygroundProps {}

export class Playground extends React.PureComponent<PlaygroundProps> {
	render() {
		return <ImportIdentity />
	}
}
